import { createPageMetadata } from "@/lib/metadata"

export const metadata = createPageMetadata({
  title: "Admin Sign In",
  description: "Sign in to the Bryker & Co. admin portal",
  path: "/admin/login",
  noIndex: true,
})
