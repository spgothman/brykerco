# Image assets

Place optimized assets here before launch:

| File | Purpose | Notes |
|------|---------|-------|
| `logo.png` | Standard logo (dark backgrounds) | PNG, transparent background |
| `logo-white.png` | Reversed logo (navy backgrounds) | Replace `logo-white.svg` when ready |
| `hero-austin.jpg` | Homepage hero | Under 300KB, 2x resolution; `next/image` optimizes delivery |
| `team/*.png` | Team headshots | Under 300KB each |

Current fallbacks: `logo-white.svg` wordmark for footer until `logo-white.png` is supplied.
